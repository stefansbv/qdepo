# QDepo
# Database connection configuration
# Valid options for driver are:
#  cubrid | firebird | postgresql | mysql | sqlite
# Output path points to the directory where output files are created
#
# ~ means undefined
---
connection:
  driver: sqlite
  host: localhost
  dbname: classicmodels.db
  port: ~
