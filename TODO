1. Safety: check if the statement is a pure select one.

2. SQL Editor/Viewer
 delete the quotes around the value if exists at execute
 put quotes around the value just for the preview

PostgreSQL:

 II SQL: SELECT *
            FROM fpimm.firme
            WHERE reprez_func = '?'

 EE Statement has no placeholders to bind at
 /home/stefan/perl5/perlbrew/perls/5.14.2t/lib/site_perl/5.14.2/TpdaQrt/Output.pm
 line 468.

CUBRID:

 No eror and no output rows (only the header row)

3. Maybe: Use a lighter module for XML parse/update instead of XML::Twig. No.

4. Maybe: Disable the "not available" output options.

5. DONE: Fix errors on nonexistent mnemonic.

6. BUG: Non ASCII mnemonic names not supported!

7. Update manual with the new GUI changes.

8. Add a test button for the connection.

9. Get and show info about all the tables and views in the database.

10.
